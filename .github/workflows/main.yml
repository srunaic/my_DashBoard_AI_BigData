name: AI Dashboard CI/CD

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Lint with flake8
      run: |
        pip install flake8
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings.
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

    - name: Test with pytest
      run: |
        pip install pytest
        # pytest # (Commented out until tests are added)

  # Cloudflare Pages typically works for static sites.
  # For Streamlit, we might need a different approach or use a custom deployment script.
  # This job is a placeholder if you are deploying a static export or using a specific Cloudflare adapter.
  deploy-cloudflare:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    permission:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      # Example: Publishing to Cloudflare Pages (Direct Upload)
      # You need CLOUDFLARE_ACCOUNT_ID and CLOUDFLARE_API_TOKEN secrets in GitHub.
      # - name: Publish to Cloudflare Pages
      #   uses: cloudflare/pages-action@v1
      #   with:
      #     apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      #     accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      #     projectName: my-dashboard # Replace with your project name
      #     directory: . # Directory to publish
      #     gitHubToken: ${{ secrets.GITHUB_TOKEN }}
